--[[
    VILLONHUB CORE - WINDUI v3 EDITION (FULL REPLICATION)
    Engine: WindUI v3 (Dark Theme)
    Target Environment: Delta / Hydrogen / Wave / All Executors
]]

if getgenv().AdvancedCoreLoaded then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "VillonHub",
        Text = "Архитектура уже active в текущей сессии.",
        Duration = 3
    })
    return
end
getgenv().AdvancedCoreLoaded = true

--// Системные сервисы
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local CoreGui = game:GetService("CoreGui")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- ===== НАСТРОЙКИ АДАПТИВНОГО ПРЕДИКШЕНА =====
local CONFIG_SM = {
    BulletSpeed = 280,
    PredictionFactor = 0.85,
    MaxHorizontalPrediction = 300,
    MaxVerticalPrediction = 50,
    PingCompensation = true,
    UseVerticalCorrection = true,
    VerticalCorrectionFactor = 0.3
}

--// Глобальная база конфигурации
local Options = {
    Aimbot = {
        Enabled = false,
        Radius = 120,
        TargetPart = "Torso",
        TeamCheck = true,
        WallCheck = true,
        BindButtonEnabled = false
    },
    Visuals = {
        Boxes = false,
        BoxColor = Color3.fromRGB(255, 0, 100),
        Skeletons = false,
        SkeletonColor = Color3.fromRGB(255, 255, 255),
        Names = false,
        HealthBar = false,
        Tracers = false,
        Chams = false,
        ChamsColor = Color3.fromRGB(255, 0, 100),
        TeamCheck = true
    },
    MM2 = {
        RoleESP = false,
        GunESP = false,
        TPGunButtonEnabled = false,
        ShootMurderButtonEnabled = false,
        ThrowKnifeButtonEnabled = false,
        InvisButtonEnabled = false,
        FlingButtonEnabled = false,
        AimMurderOnly = false,
        AutoAimMurder = false,
        Invisibility = false,
        Fling = false,
        AutoTPGun = false,
        KillAllButtonEnabled = false,
        KillSheriffButtonEnabled = false,
        JerkOffEnabled = false
    },
    Misc = {
        SpinBot = false,
        WalkSpeedEnabled = false,
        WalkSpeedValue = 16,
        AntiFling = true,
        DoubleJump = false,
        FovEnabled = false,
        FovValue = 70,
        AspectRatioEnabled = false,
        AspectRatioValue = 1.33,
        SpeedGlitchEnabled = false,
        SpeedGlitchValue = 20
    }
}

--// Состояния и кэш
local ScreenCenter = Camera.ViewportSize / 2
local Connections = {}
local jumpPressed = false
local doubleJumpCount = 0
local env = { OldPos = nil, timeout = 2.5 }
local cachedRoles = {}

--// FOV Кольцо
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1
FOVCircle.NumSides = 40
FOVCircle.Radius = Options.Aimbot.Radius
FOVCircle.Filled = false
FOVCircle.Visible = false
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Position = ScreenCenter

table.insert(Connections, Camera:GetPropertyChangedSignal("ViewportSize"):Connect(function()
    ScreenCenter = Camera.ViewportSize / 2
    FOVCircle.Position = ScreenCenter
end))

--// Вспомогательные функции
local function getRoot(char)
    if not char then return nil end
    return char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
end

local function getPing()
    if LocalPlayer and LocalPlayer.GetNetworkPing then
        return LocalPlayer:GetNetworkPing() * 1000
    end
    return 0
end

local function showVillonNotice(txt)
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "VillonHub",
        Text = txt,
        Duration = 3
    })
end

--// Double Jump
local function doDoubleJumpLogic()
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if hum and Options.Misc.DoubleJump then
        if hum:GetState() == Enum.HumanoidStateType.Freefall and doubleJumpCount < 1 then
            hum:ChangeState(Enum.HumanoidStateType.Jumping)
            doubleJumpCount = 1
        end
    end
end

table.insert(Connections, UserInputService.InputBegan:Connect(function(input, processed)
    if processed then return end
    if input.KeyCode == Enum.KeyCode.Space then
        jumpPressed = true
        doDoubleJumpLogic()
    end
end))

table.insert(Connections, UserInputService.InputEnded:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Space then jumpPressed = false end
end))

table.insert(Connections, RunService.PostSimulation:Connect(function()
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    if hum and hum.FloorMaterial ~= Enum.Material.Air then
        doubleJumpCount = 0
    end
end))

--// MM2 Роли
local function getRoles()
    local success, data = pcall(function()
        return ReplicatedStorage:FindFirstChild("GetPlayerData", true):InvokeServer()
    end)
    if not success or not data then return {} end
    local roles = {}
    for plr, plrData in pairs(data) do
        if not plrData.Dead then roles[plr] = plrData.Role end
    end
    return roles
end

task.spawn(function()
    while true do
        if Options.MM2.RoleESP or Options.MM2.AimMurderOnly or Options.MM2.AutoAimMurder then
            cachedRoles = getRoles()
        end
        task.wait(0.25)
    end
end)

local function AmIMurderer()
    return cachedRoles[LocalPlayer.Name] == "Murderer"
end

local function equipKnife()
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if hum and backpack and backpack:FindFirstChild("Knife") then
        hum:EquipTool(backpack.Knife)
        return true
    end
    return false
end

local function equipGun()
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if hum and backpack and backpack:FindFirstChild("Gun") then
        hum:EquipTool(backpack.Gun)
        return true
    end
    return false
end

local function getClosestPlayer()
    local closest, minDist = nil, math.huge
    local myChar = LocalPlayer.Character
    local myRoot = getRoot(myChar)
    if not myRoot then return nil end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local root = getRoot(p.Character)
            local hum = p.Character:FindFirstChildOfClass("Humanoid")
            if root and hum and hum.Health > 0 then
                local dist = (root.Position - myRoot.Position).Magnitude
                if dist < minDist then
                    minDist = dist
                    closest = p
                end
            end
        end
    end
    return closest
end

--// Выстрел в Убийцу с предикшеном
local function getMurdererTarget()
    local roles = getRoles()
    local myChar = LocalPlayer.Character
    local myPos = myChar and myChar:FindFirstChild("HumanoidRootPart") and myChar.HumanoidRootPart.Position
    if not myPos then return nil end

    local bestTarget, bestScore = nil, math.huge
    for plr, role in pairs(roles) do
        if role == "Murderer" and plr ~= LocalPlayer.Name then
            local player = Players:FindFirstChild(plr)
            if player and player.Character then
                local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local currentPos = hrp.Position
                    local velocity = hrp.AssemblyLinearVelocity
                    local velXZ = Vector3.new(velocity.X, 0, velocity.Z)
                    local velY = math.clamp(velocity.Y, -10, 10)

                    local dist = (currentPos - myPos).Magnitude
                    local travelTime = dist / CONFIG_SM.BulletSpeed
                    if CONFIG_SM.PingCompensation then
                        travelTime = travelTime + math.min(getPing() / 1000 * 0.5, 0.08)
                    end

                    local horizPred = velXZ * travelTime * CONFIG_SM.PredictionFactor
                    if horizPred.Magnitude > CONFIG_SM.MaxHorizontalPrediction then
                        horizPred = horizPred.Unit * CONFIG_SM.MaxHorizontalPrediction
                    end

                    local vertPred = Vector3.new(0, velY * travelTime * CONFIG_SM.PredictionFactor, 0)
                    if math.abs(vertPred.Y) > CONFIG_SM.MaxVerticalPrediction then
                        vertPred = Vector3.new(0, math.sign(vertPred.Y) * CONFIG_SM.MaxVerticalPrediction, 0)
                    end

                    local predictedPos = currentPos + horizPred + vertPred
                    if CONFIG_SM.UseVerticalCorrection and math.abs(velocity.Y) > 3 then
                        predictedPos = predictedPos + Vector3.new(0, -0.5, 0)
                    end

                    if dist < bestScore then
                        bestScore = dist
                        bestTarget = predictedPos
                    end
                end
            end
        end
    end
    return bestTarget
end

local function ShootMurdererAction()
    equipGun()
    local targetPos = getMurdererTarget()
    if targetPos then
        local char = LocalPlayer.Character
        local gun = char and char:FindFirstChild("Gun")
        if gun and gun:FindFirstChild("KnifeServer") and gun.KnifeServer:FindFirstChild("ShootGun") then
            gun.KnifeServer.ShootGun:FireServer(1, targetPos, "Shoot")
            showVillonNotice("Выстрел произведён!")
        end
    else
        showVillonNotice("Мардер не найден!")
    end
end

--// Бросок ножа
local function throwKnifeToClosest()
    local char = LocalPlayer.Character
    if not char then return end
    local knife = char:FindFirstChild("Knife") or (equipKnife() and char:FindFirstChild("Knife"))
    if not knife or not knife:FindFirstChild("Events") then return end
    local knifeThrown = knife.Events:FindFirstChild("KnifeThrown")
    local target = getClosestPlayer()
    if knifeThrown and target and target.Character then
        local targetRoot = getRoot(target.Character)
        if targetRoot then
            local targetPos = targetRoot.Position
            local vel = targetRoot.AssemblyLinearVelocity
            local myRoot = char:FindFirstChild("HumanoidRootPart")
            if myRoot then
                local dist = (targetPos - myRoot.Position).Magnitude
                local travelTime = dist / CONFIG_SM.BulletSpeed + math.min(getPing() / 1000 * 0.5, 0.1)
                local pred = vel * travelTime * CONFIG_SM.PredictionFactor
                if pred.Magnitude > CONFIG_SM.MaxHorizontalPrediction then
                    pred = pred.Unit * CONFIG_SM.MaxHorizontalPrediction
                end
                targetPos = targetPos + pred
            end
            pcall(function()
                knifeThrown:FireServer(knife.Handle.CFrame, CFrame.new(targetPos))
            end)
        end
    end
end

--// Fling (SHubFling Engine)
local function SHubFling(TargetPlayer)
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local root = getRoot(char)
    if not (char and hum and root and TargetPlayer and TargetPlayer.Character) then return end
    
    local TCharacter = TargetPlayer.Character
    local THumanoid = TCharacter:FindFirstChildOfClass("Humanoid")
    local TRootPart = THumanoid and THumanoid.RootPart
    if not TRootPart then return end

    env.OldPos = root.CFrame
    local BV = Instance.new("BodyVelocity")
    BV.Velocity = Vector3.new(9e8, 9e8, 9e8)
    BV.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    BV.Parent = root
    hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

    local start = tick()
    repeat
        root.CFrame = TRootPart.CFrame * CFrame.Angles(math.rad(math.random(0,360)), 0, 0)
        root.Velocity = Vector3.new(9e7, 9e8, 9e7)
        task.wait()
    until not TRootPart or TRootPart.Velocity.Magnitude > 500 or tick() - start > 2.5

    BV:Destroy()
    hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
    if env.OldPos then
        root.CFrame = env.OldPos
        root.Velocity = Vector3.zero
    end
end

local function FlingMurderer()
    for plr, role in pairs(cachedRoles) do
        if role == "Murderer" and plr ~= LocalPlayer.Name then
            local target = Players:FindFirstChild(plr)
            if target then SHubFling(target) return end
        end
    end
    showVillonNotice("Убийца не найден!")
end

local function FlingSheriff()
    for plr, role in pairs(cachedRoles) do
        if (role == "Sheriff" or role == "Hero") and plr ~= LocalPlayer.Name then
            local target = Players:FindFirstChild(plr)
            if target then SHubFling(target) return end
        end
    end
    showVillonNotice("Шериф не найден!")
end

--// Kill All & Kill Sheriff
local function killPlayer(targetPlayer)
    local char = LocalPlayer.Character
    local myRoot = getRoot(char)
    if not (char and myRoot and targetPlayer and targetPlayer.Character) then return end
    local targetRoot = getRoot(targetPlayer.Character)
    local targetHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
    if not (targetRoot and targetHum and targetHum.Health > 0) then return end

    equipKnife()
    local knife = char:FindFirstChild("Knife")
    local oldCF = myRoot.CFrame

    for i = 1, 5 do
        if targetHum.Health <= 0 then break end
        myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 1, 1.5)
        if knife then knife:Activate() end
        task.wait(0.08)
    end

    myRoot.CFrame = oldCF
    myRoot.Velocity = Vector3.zero
end

local function KillAll()
    if not AmIMurderer() then showVillonNotice("Вы не Убийца!") return end
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            killPlayer(p)
            task.wait(0.05)
        end
    end
end

local function KillSheriff()
    if not AmIMurderer() then showVillonNotice("Вы не Убийца!") return end
    for plr, role in pairs(cachedRoles) do
        if (role == "Sheriff" or role == "Hero") and plr ~= LocalPlayer.Name then
            local target = Players:FindFirstChild(plr)
            if target then killPlayer(target) return end
        end
    end
    showVillonNotice("Шериф не найден!")
end

--// TP Gun
local function AutoGrabGun()
    local char = LocalPlayer.Character
    local root = getRoot(char)
    if not (char and root) then return end
    local gun = Workspace:FindFirstChild("GunDrop", true)
    if gun then
        if firetouchinterest then
            firetouchinterest(root, gun, 0)
            firetouchinterest(root, gun, 1)
        else
            gun.CFrame = root.CFrame
        end
    end
end

task.spawn(function()
    while true do
        if Options.MM2.AutoTPGun and not AmIMurderer() then AutoGrabGun() end
        task.wait(0.1)
    end
end)

--// Невидимость
local function setCharacterTransparency(char, val)
    for _, p in ipairs(char:GetDescendants()) do
        if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" then
            p.Transparency = val
        end
    end
end

local function toggleInvisibilityLogic(state)
    Options.MM2.Invisibility = state
    local char = LocalPlayer.Character
    if char then
        if state then
            setCharacterTransparency(char, 0.5)
            local savedpos = char.HumanoidRootPart.CFrame
            task.wait()
            char:MoveTo(Vector3.new(-25.95, 84, 3537.55))
            task.wait(0.15)
            local Seat = Instance.new("Seat", workspace)
            Seat.Anchored, Seat.CanCollide, Seat.Name, Seat.Transparency = false, false, "invischair", 1
            Seat.Position = Vector3.new(-25.95, 84, 3537.55)
            local Weld = Instance.new("Weld", Seat)
            Weld.Part0, Weld.Part1 = Seat, char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
            Seat.CFrame = savedpos
            showVillonNotice("Невидимость ВКЛ")
        else
            setCharacterTransparency(char, 0)
            if workspace:FindFirstChild("invischair") then workspace.invischair:Destroy() end
            showVillonNotice("Невидимость ВЫКЛ")
        end
    end
end

-- =================================================================
-- ИНИЦИАЛИЗАЦИЯ ИНТЕРФЕЙСА (WINDUI v3)
-- =================================================================
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "VillonHub",
    Author = "Villon",
    Folder = "VillonConfig",
    Size = UDim2.fromOffset(600, 480),
    Transparent = true,
    Theme = "Dark",
})

Window:EditOpenButton({
    Title = "VillonHub",
    Icon = "shield",
    CornerRadius = UDim.new(0, 10),
    StrokeThickness = 2,
    Color = ColorSequence.new(Color3.fromRGB(35, 35, 45), Color3.fromRGB(15, 15, 25))
})

-- ==================== ВСТРОЕННЫЕ НАСТРАИВАЕМЫЕ МОБИЛЬНЫЕ КНОПКИ ====================
local TouchGui = Instance.new("ScreenGui")
TouchGui.Name = "VillonHub_MobileButtons"
TouchGui.ResetOnSpawn = false
TouchGui.Parent = gethui and gethui() or CoreGui

local function createFloatingButton(name, text, defaultPos, callback)
    local btn = Instance.new("TextButton")
    btn.Name = name
    btn.Size = UDim2.new(0, 70, 0, 70)
    btn.Position = defaultPos
    btn.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 12
    btn.Visible = false
    btn.Active = true
    btn.Draggable = false
    btn.Parent = TouchGui

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = btn

    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(0, 150, 255)
    stroke.Thickness = 2
    stroke.Parent = btn

    btn.MouseButton1Click:Connect(callback)
    return btn
end

local AimBtn = createFloatingButton("Aim_BindButton", "AIM", UDim2.new(0.8, 0, 0.4, 0), function()
    local targetPos = getMurdererTarget()
    if targetPos then Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPos) end
end)

local TPGunBtn = createFloatingButton("MM2_TPGunButton", "TP GUN", UDim2.new(0.8, 0, 0.52, 0), function()
    AutoGrabGun()
end)

local ShootMurderBtn = createFloatingButton("MM2_ShootMurderButton", "SHOOT\nMURDER", UDim2.new(0.8, 0, 0.64, 0), function()
    ShootMurdererAction()
end)

local ThrowKnifeBtn = createFloatingButton("MM2_ThrowKnifeButton", "THROW\nKNIFE", UDim2.new(0.8, 0, 0.76, 0), function()
    throwKnifeToClosest()
end)

local InvisBtn = createFloatingButton("MM2_InvisButton", "INVIS", UDim2.new(0.7, 0, 0.4, 0), function()
    toggleInvisibilityLogic(not Options.MM2.Invisibility)
end)

local FlingBtn = createFloatingButton("MM2_FlingButton", "FLING", UDim2.new(0.7, 0, 0.52, 0), function()
    FlingMurderer()
end)

local KillAllBtn = createFloatingButton("KillAllButton", "KILL ALL", UDim2.new(0.7, 0, 0.64, 0), function()
    KillAll()
end)

local KillSheriffBtn = createFloatingButton("KillSheriffButton", "KILL\nSHERIFF", UDim2.new(0.7, 0, 0.76, 0), function()
    KillSheriff()
end)

-- Функция включения/выключения режима перетаскивания (⚙)
local function toggleButtonDrag(btn, state)
    btn.Draggable = state
    btn.BackgroundColor3 = state and Color3.fromRGB(0, 150, 255) or Color3.fromRGB(25, 25, 30)
    showVillonNotice(state and ("Режим настройки: Перетащите " .. btn.Text) or ("Положение " .. btn.Text .. " зафиксировано."))
end

-- ==================== ВКЛАДКА COMBAT ====================
local CombatTab = Window:Tab({ Title = "Combat (AIM)", Icon = "crosshair" })

CombatTab:Toggle({
    Title = "Aimbot Enabled",
    Desc = "Включить Аимбот",
    Value = Options.Aimbot.Enabled,
    Callback = function(v) Options.Aimbot.Enabled = v; FOVCircle.Visible = v end
})

CombatTab:Slider({
    Title = "FOV Radius",
    Min = 30, Max = 500, Value = Options.Aimbot.Radius,
    Callback = function(v) Options.Aimbot.Radius = v; FOVCircle.Radius = v end
})

CombatTab:Dropdown({
    Title = "Target Part",
    Values = {"Torso", "Head", "HumanoidRootPart"},
    Value = Options.Aimbot.TargetPart,
    Callback = function(v) Options.Aimbot.TargetPart = v end
})

CombatTab:Toggle({
    Title = "Aim Murder Only",
    Desc = "Целиться только в Мардера",
    Value = Options.MM2.AimMurderOnly,
    Callback = function(v) Options.MM2.AimMurderOnly = v end
})

CombatTab:Toggle({
    Title = "AIM Bind Button (⚙)",
    Desc = "Экранная кнопка Аима",
    Value = Options.Aimbot.BindButtonEnabled,
    Callback = function(v) AimBtn.Visible = v end
})
CombatTab:Button({ Title = "⚙ Перетащить AIM кнопку", Callback = function() toggleButtonDrag(AimBtn, not AimBtn.Draggable) end })

-- ==================== ВКЛАДКА VISUALS ====================
local VisualsTab = Window:Tab({ Title = "Visuals (ESP)", Icon = "eye" })

VisualsTab:Toggle({ Title = "Player Boxes", Value = Options.Visuals.Boxes, Callback = function(v) Options.Visuals.Boxes = v end })
VisualsTab:Toggle({ Title = "Player Names", Value = Options.Visuals.Names, Callback = function(v) Options.Visuals.Names = v end })
VisualsTab:Toggle({ Title = "Tracers", Value = Options.Visuals.Tracers, Callback = function(v) Options.Visuals.Tracers = v end })
VisualsTab:Toggle({ Title = "Chams", Value = Options.Visuals.Chams, Callback = function(v) Options.Visuals.Chams = v end })
VisualsTab:Toggle({ Title = "MM2 Role ESP", Value = Options.MM2.RoleESP, Callback = function(v) Options.MM2.RoleESP = v end })
VisualsTab:Toggle({ Title = "MM2 Dropped Gun ESP", Value = Options.MM2.GunESP, Callback = function(v) Options.MM2.GunESP = v end })

-- ==================== ВКЛАДКА MM2 ====================
local MM2Tab = Window:Tab({ Title = "MM2 Specials", Icon = "swords" })

MM2Tab:Button({ Title = "Shoot Murderer (Auto-Shot)", Desc = "Выстрелить с предикшеном", Callback = ShootMurdererAction })
MM2Tab:Toggle({ Title = "Shoot Murderer Button (⚙)", Value = Options.MM2.ShootMurderButtonEnabled, Callback = function(v) ShootMurderBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить Shoot Murder кнопкy", Callback = function() toggleButtonDrag(ShootMurderBtn, not ShootMurderBtn.Draggable) end })

MM2Tab:Button({ Title = "Throw Knife to Closest", Callback = throwKnifeToClosest })
MM2Tab:Toggle({ Title = "Throw Knife Button (⚙)", Value = Options.MM2.ThrowKnifeButtonEnabled, Callback = function(v) ThrowKnifeBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить Throw Knife кнопку", Callback = function() toggleButtonDrag(ThrowKnifeBtn, not ThrowKnifeBtn.Draggable) end })

MM2Tab:Toggle({ Title = "Auto Grab Gun", Value = Options.MM2.AutoTPGun, Callback = function(v) Options.MM2.AutoTPGun = v end })
MM2Tab:Toggle({ Title = "TP Gun Button (⚙)", Value = Options.MM2.TPGunButtonEnabled, Callback = function(v) TPGunBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить TP Gun кнопку", Callback = function() toggleButtonDrag(TPGunBtn, not TPGunBtn.Draggable) end })

MM2Tab:Toggle({ Title = "Invisibility (Chair)", Value = Options.MM2.Invisibility, Callback = function(v) toggleInvisibilityLogic(v) end })
MM2Tab:Toggle({ Title = "Invis Button (⚙)", Value = Options.MM2.InvisButtonEnabled, Callback = function(v) InvisBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить Invis кнопку", Callback = function() toggleButtonDrag(InvisBtn, not InvisBtn.Draggable) end })

MM2Tab:Button({ Title = "Fling Murderer", Callback = FlingMurderer })
MM2Tab:Button({ Title = "Fling Sheriff", Callback = FlingSheriff })
MM2Tab:Toggle({ Title = "Fling Button (⚙)", Value = Options.MM2.FlingButtonEnabled, Callback = function(v) FlingBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить Fling кнопку", Callback = function() toggleButtonDrag(FlingBtn, not FlingBtn.Draggable) end })

MM2Tab:Button({ Title = "Kill All (For Murderer)", Callback = KillAll })
MM2Tab:Toggle({ Title = "Kill All Button (⚙)", Value = Options.MM2.KillAllButtonEnabled, Callback = function(v) KillAllBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить Kill All кнопку", Callback = function() toggleButtonDrag(KillAllBtn, not KillAllBtn.Draggable) end })

MM2Tab:Button({ Title = "Kill Sheriff (For Murderer)", Callback = KillSheriff })
MM2Tab:Toggle({ Title = "Kill Sheriff Button (⚙)", Value = Options.MM2.KillSheriffButtonEnabled, Callback = function(v) KillSheriffBtn.Visible = v end })
MM2Tab:Button({ Title = "⚙ Перетащить Kill Sheriff кнопку", Callback = function() toggleButtonDrag(KillSheriffBtn, not KillSheriffBtn.Draggable) end })

-- ==================== ВКЛАДКА CHANGE FLING ====================
local ChangeFlingTab = Window:Tab({ Title = "Change Fling", Icon = "user-x" })

local playerFlingDropdown
local selectedPlayerToFling = nil

local function updatePlayerFlingList()
    local plist = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then table.insert(plist, p.Name) end
    end
    if playerFlingDropdown then
        playerFlingDropdown:SetValues(plist)
    end
end

playerFlingDropdown = ChangeFlingTab:Dropdown({
    Title = "Выберите игрока для Fling",
    Values = {},
    Callback = function(v) selectedPlayerToFling = v end
})

ChangeFlingTab:Button({
    Title = "Обновить список игроков",
    Callback = updatePlayerFlingList
})

ChangeFlingTab:Button({
    Title = "Зафлинжить выбранного",
    Callback = function()
        if selectedPlayerToFling then
            local target = Players:FindFirstChild(selectedPlayerToFling)
            if target then SHubFling(target) else showVillonNotice("Игрок не найден!") end
        else
            showVillonNotice("Сначала выберите игрока!")
        end
    end
})
updatePlayerFlingList()

-- ==================== ВКЛАДКА MISC & SETTINGS ====================
local MiscTab = Window:Tab({ Title = "Misc & Settings", Icon = "sliders" })

MiscTab:Toggle({ Title = "Double Jump", Value = Options.Misc.DoubleJump, Callback = function(v) Options.Misc.DoubleJump = v end })
MiscTab:Toggle({ Title = "Anti-Fling", Value = Options.Misc.AntiFling, Callback = function(v) Options.Misc.AntiFling = v end })

MiscTab:Toggle({ Title = "SpinBot", Value = Options.Misc.SpinBot, Callback = function(v) Options.Misc.SpinBot = v end })
MiscTab:Toggle({ Title = "JerkOff Animation", Value = Options.MM2.JerkOffEnabled, Callback = function(v) Options.MM2.JerkOffEnabled = v end })

MiscTab:Toggle({ Title = "Custom WalkSpeed", Value = Options.Misc.WalkSpeedEnabled, Callback = function(v) Options.Misc.WalkSpeedEnabled = v end })
MiscTab:Slider({ Title = "WalkSpeed Value", Min = 16, Max = 120, Value = Options.Misc.WalkSpeedValue, Callback = function(v) Options.Misc.WalkSpeedValue = v end })

MiscTab:Toggle({ Title = "Speed Glitch", Value = Options.Misc.SpeedGlitchEnabled, Callback = function(v) Options.Misc.SpeedGlitchEnabled = v end })
MiscTab:Slider({ Title = "Speed Glitch Value", Min = 10, Max = 100, Value = Options.Misc.SpeedGlitchValue, Callback = function(v) Options.Misc.SpeedGlitchValue = v end })

MiscTab:Toggle({ Title = "Custom FOV", Value = Options.Misc.FovEnabled, Callback = function(v) Options.Misc.FovEnabled = v end })
MiscTab:Slider({ Title = "FOV Value", Min = 50, Max = 120, Value = Options.Misc.FovValue, Callback = function(v) Options.Misc.FovValue = v end })

MiscTab:Button({
    Title = "Unload VillonHub",
    Callback = function()
        for _, conn in ipairs(Connections) do if conn then conn:Disconnect() end end
        FOVCircle:Remove()
        if workspace:FindFirstChild("invischair") then workspace.invischair:Destroy() end
        TouchGui:Destroy()
        Window:Destroy()
        getgenv().AdvancedCoreLoaded = nil
        showVillonNotice("VillonHub полностью выгружен.")
    end
})

-- ==================== ГЛАВНЫЙ ЦИКЛ ====================
table.insert(Connections, RunService.RenderStepped:Connect(function()
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local root = getRoot(char)

    if hum and Options.Misc.WalkSpeedEnabled then
        hum.WalkSpeed = Options.Misc.WalkSpeedValue
    end

    if Options.Misc.FovEnabled then
        Camera.FieldOfView = Options.Misc.FovValue
    end

    if Options.Misc.SpinBot and root then
        root.CFrame = root.CFrame * CFrame.Angles(0, math.rad(50), 0)
    end

    if Options.Misc.AntiFling and char then
        for _, part in ipairs(char:GetChildren()) do
            if part:IsA("BasePart") then
                part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
            end
        end
    end
end))

showVillonNotice("VillonHub v3 с полным функционалом успешно загружен!")